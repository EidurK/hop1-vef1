{
  "name": "hop1-vef1",
  "version": "1.0.0",
  "description": "Hópaverkefni?",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass styles.scss styles.css",
    "sass-watch": "sass --watch styles.scss style.css",
    "browser-sync": "browser-sync start --server --files index.html *.css",
    "dev": "concurrently \"npm:sass-watch\" \"npm:browser-sync start --server --files ./scripts/hamburger.js index.html\"",
    "lint": "stylelint styles.scss './styles/*.scss'",
    "lint-fix": "stylelint styles.scss './styles/*.scss' --fix"
  },
  "author": "Eidur",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^3.0.3",
    "concurrently": "^9.0.1",
    "sass": "^1.79.4",
    "stylelint": "^16.9.0",
    "stylelint-config-sass-guidelines": "^12.1.0",
    "stylelint-config-standard": "^36.0.1"
  }
}
